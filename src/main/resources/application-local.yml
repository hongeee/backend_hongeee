spring:
  datasource:
    url: jdbc:mysql://localhost:3306/request_vacation
    username: root
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  jwt:
    secret: changeit